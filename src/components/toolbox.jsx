function Toolbox({ conversation }) {
  return (
    <div
      style={{
        background: "red",
        color: "white",
        padding: "20px",
        borderRadius: "12px",
        fontWeight: "bold"
      }}
    >
      🚨 THIS IS THE TOOLBOX COMPONENT 🚨
    </div>
  );
}

export default Toolbox;